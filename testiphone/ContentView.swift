import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("Sale gay")
        }
    }
}
#Preview {
    ContentView()
    
   
}
