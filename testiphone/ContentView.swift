import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("Annaeg X Karl")
            Text("La racaile du japon")
        }
    }
}
#Preview {
    ContentView()
    
   
}
