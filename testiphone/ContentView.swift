import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("Annaeg X Karl")
            Text("coucou mon bébou")
        }
    }
}
#Preview {
    ContentView()
    
   
}
