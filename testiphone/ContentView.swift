import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("Sala malecoum abibi")
        }
    }
}
#Preview {
    ContentView()
    
   
}
