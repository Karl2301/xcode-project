import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("J'aime les bites")
        }
    }
}
#Preview {
    ContentView()
    
   
}
