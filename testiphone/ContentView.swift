import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(){
            Image(systemName: "globe")
            Text("Annaeg X Karl")
            Text("Xcode 2024")
        }
    }
}
#Preview {
    ContentView()
    
   
}
